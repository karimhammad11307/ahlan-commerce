procs:
  redis:
    shell: ".bin/host-exec redis-cli monitor"
  api:
    shell: "make run-api"
  postgres:
    shell: "make db-start && .bin/host-exec docker logs -f ahlan_db"
  admin:
    shell: "cd apps/admin && npm run dev"
  worker:
    shell: "make run-worker"
