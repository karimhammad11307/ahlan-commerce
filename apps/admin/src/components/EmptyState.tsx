/** Empty-state callout rendered when the product list has zero items. */
export function EmptyState() {
  return (
    <p role="status">
      No products yet. Use the form below to create the first one.
    </p>
  )
}
